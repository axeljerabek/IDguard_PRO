import cv2; import numpy as np; import time; from datetime import datetime; path='/workspace/IDguard_PRO_COMPLETE/alerts/FINAL_SUCCESS_' + datetime.datetime.now().strftime('%H%M%S') + '.mp4'; out=cv2.VideoWriter(path, cv2.VideoWriter_fourcc(*'mp4v'), 15, (640, 480)); start=time.time(); [out.write(np.zeros((480, 640, 3), dtype=np.uint8)) for _ in range(75)]; out.release(); print('DELIVER:' + path)